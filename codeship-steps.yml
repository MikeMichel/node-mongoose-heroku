- name: start node
  service: node
  command: npm start

- name: push it
  type: push
  image_name: mikemichel/node-mongoose-sloppyio
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
